config MISC_DUAL_SIM_PLUG
   bool "Sprd Dual sim plug driver"
   default n
   depends on SPRD_SIPC
   help
     This driver supports the Spreadtrum dual sim plug.
     Say Y here if you want to use it.
     It depends on sipc.
     But it is in driver/misc.
